let score = "50abc";
console.log(typeof(score));

let scoreInNumber = Number(score);
console.log(typeof(scoreInNumber));
console.log(scoreInNumber);

//NaN in the output means "not a number", which comes when you assign a variable which is not actually a number but you have assigned it as a number

